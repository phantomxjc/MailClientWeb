#!/bin/bash
# 星尘邮箱 —— 用户在应用设置页修改环境变量之后执行
#
# 环境变量（同步间隔、访问密码等）改动后需要容器重新创建才会生效，
# 飞牛会在保存后重建容器；这里只记录一条日志便于排查。

LOG_DIR="${TRIM_PKGVAR}"
[ -d "$LOG_DIR" ] || LOG_DIR="/tmp"
echo "[$(date +'%Y-%m-%d %H:%M:%S')] ===== config_callback：配置已更新，容器将按新配置重建 =====" >> "${LOG_DIR}/build.log"

exit 0
